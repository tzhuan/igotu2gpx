To compile igotu2gpx on a Linux system, you need:
- qt4
- boost
- boost-program-options
- libusb
- chrpath (optional, makes it possible to run the program without installation)

On a Ubuntu system, this should get you the dependencies:
  sudo aptitude install libqt4-dev libboost-dev libboost-program-options-dev libusb-dev chrpath

Afterwards you can compile with:
  qmake
  make
  sudo make install

If you get error messages about "Unknown test functions" from qmake, try
qmake-qt4 instead.

To get started, try:
  man igotu2gpx
  igotu2gpx info
  igotu2gpx dump
