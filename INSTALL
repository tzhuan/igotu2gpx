To compile igotu2gpx, you need:
- qt4
- boost
- boost-program-options
- chrpath (optional, makes it possible to run the program without installation)

On a Ubuntu system, this should get you the dependencies:
  sudo aptitude install libqt4-dev libboost-dev libboost-program-options-dev chrpath

Afterwards you can compile with:
  qmake
  make
  sudo make install

To get started, try:
  man igotu2gpx
  igotu2gpx info
  igotu2gpx dump
